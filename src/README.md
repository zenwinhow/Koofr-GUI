# Frontend (React / TypeScript)

- `components/`: 应用外壳和可复用 UI 组件。
- `features/files/`: 文件工作区与演示数据。
- `features/transfers/`: 传输队列界面。
- `services/`: 预留的类型化 Tauri 命令封装层；当前尚未接入。
- `types/`: 前端领域类型。

当前页面使用本地 React 状态演示交互，不处理凭据、Vault Safe Key、文件系统或网络请求。
